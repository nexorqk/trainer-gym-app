import { Box, Typography } from "@mui/material";

export const Content = () => {
  return (
    <Box>
      <Typography fontSize={20}>Content</Typography>
      <Typography variant="h1">Private route</Typography>
    </Box>
  );
};
